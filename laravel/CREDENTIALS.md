# Default Test Credentials

These accounts are automatically seeded into the database for testing purposes.

| Role | Username (Email) | Password |
|---|---|---|
| **Admin** | `admin@example.com` | `password123` |
| **Staff** | `staff@example.com` | `password123` |
| **Patient** | `patient@example.com` | `password123` |

> [!IMPORTANT]
> These credentials are for **local development and testing only**. Ensure they are changed or removed before deploying to a production environment.
