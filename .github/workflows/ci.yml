name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  laravel:
    name: Laravel Test and Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: laravel

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Docker containers
        run: docker compose build

      - name: Start Docker containers
        run: docker compose up -d db php

      - name: Prepare Laravel environment
        run: docker compose exec -T php sh -c "cp .env.pipeline .env && php artisan key:generate"

      - name: Install Composer dependencies
        run: docker compose exec -T php composer install --no-interaction --prefer-dist

      - name: Run Laravel migrations
        run: docker compose exec -T php php artisan migrate --force

      - name: Run Laravel tests
        run: docker compose exec -T php php artisan test

      - name: Show Docker logs on failure
        if: failure()
        run: docker compose logs db php

      - name: Stop Docker containers
        if: always()
        run: docker compose down -v

  flutter:
    name: Flutter Test and Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Run Flutter automated tests
        run: flutter test

      - name: Build Android debug APK
        run: flutter build apk --debug